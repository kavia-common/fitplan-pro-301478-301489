# PostgreSQL Database Configuration
# Note: The actual .env file will be populated by the orchestrator with real values
# The application will fallback to db_connection.txt if these are not set

# Option 1: Full PostgreSQL URL
POSTGRES_URL=postgresql://username:password@host:port/database

# Option 2: Individual PostgreSQL connection parameters
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123
POSTGRES_DB=myapp
POSTGRES_HOST=localhost
POSTGRES_PORT=5000

# Application Configuration
# Add any additional application-specific environment variables here
